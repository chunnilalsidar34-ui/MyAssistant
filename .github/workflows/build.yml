name: Build MyAssistant APK

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10'

      - name: Extract project ZIP
        run: |
          unzip -q MyAssistant-Android-Project.zip
          ls -la
          ls -la MyAssistant

      - name: Build APK
        working-directory: MyAssistant
        run: gradle assembleDebug --no-daemon

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: MyAssistant-APK
          path: MyAssistant/app/build/outputs/apk/debug/app-debug.apk
